# Logstash
#
description     "Logstash"

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

exec java -jar <%= node['logstash']['install_path'] %>/logstash.jar agent --config <%= node['logstash']['config_path'] %> --log <%= node['logstash']['log_path'] %>/logstash.log
